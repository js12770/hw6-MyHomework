extends layout

block content
    div.container
        div.row
            blockquote #{answer.content}
            form(method="post")
                input(type="text", name="score")
                button(type="submit", class="btn btn-default", required) Submit score