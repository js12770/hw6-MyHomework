extends layout

block content

	.header Welcom to my homework site!
	a(href='/signout', id="signout",class='text-center new-account') Sign Out
	span.username #{user.username} (#{user.identi})
	ul(class ="nav nav-tabs" )
		li(id="nav1" class='active' )
			a 首页
		li(id = "nav2")
			a 作业
	div(class='container' id='part1')
		-if( user.identi  == 'teacher')
			button(class='btn btn-primary' id="add") Add
		.model
			-if( user.identi  == 'teacher')
				button(class='edit') Edit
			div(class="panel panel-default homework_demand")
				.panel-heading 
					span.hwN New Assignment
					-if( user.identi  == 'teacher')
						input(type='text', name='hwname', class='form-control short', placeholder='New Assignment',required, autofocus) 
				.panel-body 
					-if( user.identi  == 'teacher')
						textarea 
					p.myp MyHomework是一个基于ExpressJS的Web 2.0应用，老师可以发布作业，学生可以提交作业。角色: 学生，老师。访问管理：只有选定了本课程老师和学生才能够访问使用本系统。老师可以看到所有的作业要求和所有学生提交的作业。学生能看到所有的作业要求，但只能够看到自己的作业。发布作业要求：老师可以发布作业要求，也可以修改一个已发布但是尚未截止的作业要求。提交作业：学生可以提交作业（可以多次提交作业，系统将保留最新的版本）。deadline：老师可以设定/修改作业要求的截止时间，截止时间到达后，任何学生都将无法提交作业。作业评分:截止时间到达之后，老师可以批改作业给出分数。
					span.deadline 2015.4.11 20:00
					span.remain
					button(class='modify' type='submit') 发布
		
		.model
			-if( user.identi  == 'teacher')
				button(class='edit') Edit
			div(class="panel panel-default homework_demand")
				.panel-heading 
					span.hwN 
					-if( user.identi  == 'teacher')
						input(type='text', name='hwname', class='form-control short', placeholder='New Assignment',required, autofocus) 
				.panel-body 
					-if( user.identi  == 'teacher')
						textarea 
					p.myp
					button(class='modify' type='submit') 发布

	div(class='container' id='part2')
		-if( user.identi  == 'student')
			form(class='form-textarea', method='POST')
				input(type='file', name='file',multiple='multiple', placeholder='select file',class='selectfile',required) 
				textarea.my-text
				button(class='btn btn-primary my-btn', type='submit') 提交
		-if( user.identi  == 'teacher')
			form(class='form-mark', method='POST')
				texblock.hwcontent 安装mongodb并运行<br>运行：mongod --dbpath D:\mongodb\data<br>在命令行打开项目文件夹输入：<br>npm cache clean<br>npm install<br>grunt watch
				.hwfile readme.txt
				input(type='text', class='mark', name='mark', class='form-control', placeholder='评分',required, autofocus)
				button(class='btn btn-lg btn-primary' type='submit') 完成

	script(src='scripts/index.js')
	-if( user.identi  == 'teacher')
		script(src='scripts/teacher.js')
	-if( user.identi  == 'student')
		script(src='scripts/student.js')

						